\documentclass[12pt]{article}

\usepackage{amsmath}
\usepackage[margin=1in]{geometry}
\usepackage{fancyhdr}
\usepackage{amsthm}
\newtheorem{lemma}{Lemma}
\usepackage{braket}
\usepackage{amssymb}
\usepackage{tikz}
\usepackage{mathtools}
\usetikzlibrary{shapes.geometric}
\usepackage[shortlabels]{enumitem} % Consistently use enumitem for formatting

\pagestyle{fancy}
\fancyhead[l]{Hudson Benites}
\fancyhead[c]{Homework \#4}
\fancyfoot[c]{\thepage}
\renewcommand{\headrulewidth}{0.2pt}
\setlength{\headheight}{15pt}
\fancyhead[r]{\today}

\begin{document}
\begin{enumerate}[label = {\bfseries Question \arabic*:}, leftmargin=1in, itemsep=1.5em]
    \item \begin{itemize}
        \item[(a)] $[18] = \Set{4n+2 | n\in \mathbb{Z}}$.
        \item[(b)] $[31] = \Set{4n+3 | n\in \mathbb{Z}}$.
        \item[(c)] There are 4 equivalence classes. 
    \end{itemize}

    \item \begin{proof}
        We prove that $\equiv_n$ on $\mathbb{Z}$ is an equivalence relation by showing that it is reflexive, transitive, and symmetric. Suppose $n \in \mathbb{Z}^{+}$.
        \begin{description}[leftmargin=2.5cm, style=multiline, font=\bfseries]
            \item[Reflexive] Let $a$ be an arbitrary element in $\mathbb{Z}$. Clearly, $a-a=0=kn, k\in \mathbb{Z}$. So, $\forall a \in \mathbb{Z}, a \equiv_n a $.
            \item[Symmetric] Let $a,b$ be arbitrary elements of $\mathbb{Z}$. Suppose $a \equiv_n b$. By definition, $a-b=kn, k\in \mathbb{Z}$. Multiplying the equation by $-1$, we get \begin{center}$b-a=-kn$.\end{center} $-k$ is still an integer, and thus by definition, $b\equiv_n a$.
            \item[Transitive] Let $a,b,c$ be aribtrary elements of $\mathbb{Z}$. Suppose $a\equiv_n b$ and $b\equiv_n c$. By definition, we have $a-b=kn, k \in \mathbb{Z}$ and $b-c = gn, g \in \mathbb{Z}$. Then, $b=a-nk$. Subbing in, we get that \begin{center}
                $a-kn-c=gn$\\
                $a-c=(k+g)n$
            \end{center}
           By definition, we have $a\equiv_n c$.
        \end{description}
        Thus, we are done.
    \end{proof}

    \item \begin{proof}
        Suppose $a,b,c,d \in \mathbb{Z}$, and $a\equiv_n c$ and $b\equiv_n d$. Then,
        \begin{center}
            $a-c=nk, k\in \mathbb{Z}$\\ $b-d=nl, l\in \mathbb{Z}$.
        \end{center}
        Adding the equations, we get 
        \begin{center}
        $a+b-c-d=n(k+l)$.
        \end{center} Simplifying, we get \begin{center}
            $(a+b)-(c+d) = n(k+l)$.
        \end{center}
        Thus, by definition $a+b \equiv_n c+d$.
    \end{proof}

    \item \begin{itemize}
        \item[(a)] \begin{proof}
        The theorem given in the proposition is equivalent to claiming that \lq{If $ab$ is even, then $a$ is even or $b$ is even\rq}. We have proven this theorem to be true, so it is the case that if $ab \equiv_2 0$, then $a \equiv_2 0$ or $b \equiv_2 0$.
        \end{proof}
        \item[(b)] The answer is no. $10 \times 2 = 20 \equiv_4 0$, but $10 \not\equiv_4 0$ and $2 \not\equiv_4 0$.
        \item[(c)] The answer to the question will be yes when $n$ is a prime. 
    \end{itemize}
    
    \item \begin{proof} We show three requirements.
        \begin{description}[leftmargin=1cm, style=multiline, font=\bfseries\boldmath]
            \item[1] $e \in A$ is its own inverse $(e \ast e = e)$, and so it has an inverse, and is in $A^{\times}$.
            \item[2] Suppose $a\in A^{\times}$. Then, $a\in A$ and $a$ has an inverse $a^{-1} \in A$. Similarly, $a^{-1}$ has an inverse, namely $a$. So, $a^{-1} \in A$ and $a^{-1}$ is invertible, so $a^{-1} \in A^{\times}$.
            \item[3] Suppose $a,b \in A^{\times}$. Then, $a,b \in A$, and $a \ast b \in A$. Let $c = (b^{-1} \ast a^{-1})$. Then, we compute \begin{center}
                $(a\ast b)\ast c = a\ast b\ast b^{-1}\ast a^{-1} = a\ast a^{-1} = e$.
            \end{center}
            Therefore, $a\ast b$ has an inverse. Therefore, $a\ast b \in A^{\times}$.
        \end{description}
    \end{proof}

    \item \begin{proof} We first show that matrix multiplication $(\ast)$ is a unital binary operation on $\text{Mat}_{2\times 2}(\mathbb{R})$.
                \begin{description}[leftmargin=1cm, style=multiline, font=\bfseries\boldmath]
                    \item[1] We are given that $\ast$ is associative.
                    \item[2] Let $E = \begin{bmatrix}
                        1 & 0\\
                        0 & 1
                    \end{bmatrix}, A = \begin{bmatrix}
                        a & b\\
                        c & d
                    \end{bmatrix} \in \text{Mat}_{2\times 2}(\mathbb{R})$. We compute that \begin{center}
                        $AE = EA = \begin{bmatrix}
                            a & b \\
                            c & d
                        \end{bmatrix}$.
                    \end{center} Thus, $\ast$ is unital.
                \end{description}
            Next, we show that the set $\text{GL}_2(\mathbb{R})$ is actually the set of all the matrices in $\text{Mat}_{2\times 2}(\mathbb{R})$ that are invertible. Let $A = \begin{bmatrix}
                a & b \\
                c & d
            \end{bmatrix}$. We then reduce $A$ to $E$ augmented with $E$: \begin{center}
                $\left[ \begin{array}{cc|cc}
                a & b & 1 & 0 \\
                c & d & 0 & 1
                \end{array} \right]$
                $ \rightarrow \left[ \begin{array}{cc|cc}
                1 & 0 & \frac{d}{ad-bc} & \frac{-b}{ad-bc} \\
                0 & 1 & \frac{-c}{ad-bc} & \frac{a}{ad-bc}
                \end{array} \right]$\\
            \end{center}
            Let the resultant augmented matrix be $A'$. Then, we compute
            \begin{center}
            $AA' = E$ and $A'A = E$.
            \end{center} $A'$ exists only when $ad-bc\neq 0$, which is true for all $M\in \text{GL}_2(\mathbb{R})$. In other words, $\text{GL}_2(\mathbb{R}) = \Set{A \in \text{Mat}_{2\times 2}(\mathbb{R}) | A \text{ is invertible}}$. 
            \begin{lemma}
            See the result of problem 5.
            \end{lemma}
            Thus, $\text{GL}_2(\mathbb{R})$ is a group with respect to matrix multiplication.
        \end{proof}

        \item \mbox{} \begin{itemize}
            \item[(a)] \begin{proof} We show that $R$ is reflexive, symmetric, and transitive.\begin{description}[leftmargin=2.5cm, font=\bfseries\boldmath]
                \item[Reflexive] $\ast$ is unital, so there is an identity element $e$. $e \ast e = e$, so $e$ is invertible and is its own inverse. So, $a=e\ast a\ast e$. Therefore, $R$ is reflexive.
                \item[Symmetric] Let $a,b \in A$ and $aRb$. Then, we compute \begin{center}
                    $b=t^{-1}\ast a\ast t$ \\
                    $t^{-1}\ast b = a\ast t$\\
                    $t^{-1}\ast b\ast t=a$.
                \end{center} Let $t^{-1} = t'$. $t'\in A$ and is invertible. Thus, $bRa$, and $R$ is symmetric. 
                \item[Transitive] Let $a,b,c \in A$. Suppose $aRb$ and $bRc$. Then, $b=t^{-1}\ast a\ast t$ and $c=u^{-1}\ast b \ast u$ such that $t,u\in A$. Plugging in $b$ for $a$, we get that \begin{center}
                    $c=u^{-1}\ast t^{-1}\ast a\ast t \ast u$.
                \end{center} Let $v = t\ast u$. We compute that \begin{center}
                    $(t\ast u) \ast (u^{-1}\ast t^{-1}) = t \ast t^{-1} = e$.
                \end{center} Thus, $u^{-1}\ast t^{-1} = v^{-1}$, and so $c=v^{-1}\ast a\ast v, v\in A$. Thus, $R$ is transitive.
            \end{description}
            So, $R$ is an equivalence relation.
        \end{proof}

        \item[(b)] If $\ast$ is commutative, then the relation $aRb := b=t^{-1}\ast a\ast t, a,b,t\in A$ can be simplified to $b=a$. This means that two elements are related only if they are the same element. Thus, each element is its own equivalence class. Formally, the equivalence classes are given by $\Set{[a]=\{a\}|a\in A}$.
        \item[(c)] We compute that the equivalence classes of $D_6$ are the following: \begin{center}
            $[e]=\Set{e}, [r]=\Set{r, r^2}, [s]=\Set{s, sr, sr^2}$.
        \end{center}
        \end{itemize}
\end{enumerate}
\end{document}